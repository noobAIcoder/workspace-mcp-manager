# P7.1 — Tunnel-Backed MCP Session Continuity Qualification

## Status

Normative qualification and fault-isolation specification.

## Objective

Distinguish local coding-tools MCP session behavior from tunnel/control-plane/
connector behavior without weakening MCP session isolation.

Required layers:

```text
Layer 1  direct local Streamable HTTP MCP
Layer 2  tunnel-client forwarding/control when locally reproducible
Layer 3  actual ChatGPT connector/tunnel invocation
```

## Invariants

The implementation MUST NOT work around continuity failures by making these
process-global or cross-session:

```text
default_cwd
exec_command process sessions
write_stdin access
read_output access
kill_session access
```

A process/session created under one MCP session MUST NOT become accessible from
an unrelated MCP session merely to compensate for tunnel routing behavior.

## Local control contract

Add a non-secret local session-continuity projection equivalent to:

```text
workspace-mcp-manager instance session-continuity <instance-id>
```

The operation MUST execute through the host boundary against the configured
local MCP endpoint and use one returned `Mcp-Session-Id` for all subsequent
requests.

It MUST prove at least:

```text
initialize obtains session ID
notifications/initialized succeeds
set_default_cwd(<known subdirectory>) succeeds
get_default_cwd returns the same value
exec_command starts a persistent process
write_stdin reaches that process
read_output observes process output
kill_session can terminate it
DELETE closes the MCP session
```

Raw MCP session IDs MUST NOT be returned or persisted. A short SHA-256
fingerprint MAY be reported as non-secret correlation evidence.

## Versioned projection

```text
session_continuity_projection_version = 1
```

Conceptual result:

```json
{
  "session_continuity_projection_version": 1,
  "instance_id": "electrocad",
  "scope": "local_mcp",
  "server": {"version": "0.2.2", "protocol": "2025-11-25"},
  "mcp_session": "passed",
  "default_cwd": "passed",
  "command_session": "passed",
  "cleanup": "passed",
  "session_fingerprint": "<short sha256>",
  "result": "passed"
}
```

## Tunnel-backed qualification

The actual connector/plugin layer MUST separately prove:

```text
set_default_cwd("docs")
get_default_cwd() == "docs"

exec_command -> running session_id
write_stdin(session_id) succeeds
read_output(session_id) succeeds
kill_session(session_id) succeeds
```

If Layer 1 passes but Layer 3 creates a new coding-tools runtime/session per
tool call, classify:

```text
LOCAL_MCP_SESSION_CONTINUITY=PASS
TUNNEL_BACKED_SESSION_CONTINUITY=FAIL
FAULT_DOMAIN=UPSTREAM_TUNNEL_CONTROL_PLANE_OR_CONNECTOR
```

until Layer 2 narrows the upstream boundary further.

## Evidence safety

Qualification evidence MAY include:

```text
tunnel-client version/git SHA
coding-tools-mcp version
MCP protocol version
tunnel ID
channel
timestamps
session fingerprints
pass/fail per operation
```

It MUST NOT contain:

```text
control-plane API keys
GitHub credentials
raw Mcp-Session-Id values
authorization headers
credential-bearing environment dumps
```
