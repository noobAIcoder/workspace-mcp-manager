# P7.1 — Stateless Client Compatibility Status

Status: **IMPLEMENTATION QUALIFIED — ELECTROCAD 0.3.0 STATELESS COMMAND HANDLES PASS**

## Corrected interpretation

The original P7.1 report treated loss of `Mcp-Session-Id` continuity across
ChatGPT tool invocations as a hosted tunnel/control-plane defect. That
interpretation is superseded.

Comparison with other working coding-tools instances demonstrated that
`default_cwd` commonly resets between ChatGPT tool calls without preventing
ordinary repository work. More importantly, the protocol direction after MCP
`2025-11-25` is stateless: cross-call state should be represented by explicit
server-minted handles rather than by requiring one protocol session to survive
all calls.

The relevant qualification question is therefore:

```text
Can ordinary atomic tools complete without mutable protocol-session state?

If a tool returns an explicit process/output handle, can a fresh MCP session
use that handle?
```

## Live ElectroCAD result

The revised manager probe against the deployed ElectroCAD local MCP returned:

```text
coding-tools-mcp                 0.3.0
protocol                         2025-11-25
atomic_coding_ready              true
protocol_session_persistence     not_required_for_atomic_operations
transport_session_model          stateless

process handle                   command_id
termination tool                 kill_command

explicit relative workdir        PASS
later-call write_stdin           cross_session
later-call read_output           cross_session
later-call kill_command          cross_session
command handle classification    cross_session

cleanup                          not_applicable
result                           passed
```

No raw MCP protocol-session or process-handle values are retained in evidence.

## What passes

The following capability class is fully usable without requiring one persistent
MCP protocol session:

```text
repository discovery and metadata
read_file / list_files / search_text
apply_patch
Git inspection
bounded exec_command
Node/npm/pnpm repository commands that finish within one invocation
explicit path/cwd/workdir operation
```

This is the ordinary coding-readiness class used by `ElectroCADCodingTools` and
other coding-tools instances.

## Historical 0.2.x compatibility

The manager continues to qualify coding-tools 0.2.2. That runtime scopes the
explicit process/output handles returned by `exec_command` to the creating MCP
protocol session.

Therefore a client that starts a fresh MCP session for its next tool invocation
cannot currently perform:

```text
exec_command -> write_stdin on a later call
exec_command -> kill_session on a later call
output_ref   -> read_output on a later call
```

The observed response is `SESSION_NOT_FOUND`.

That historical result remains `passed_with_limitations`; it is **not** evidence
that ordinary MCP coding requires protocol-session persistence and is not
classified as a hosted tunnel fault.

## coding-tools 0.3.0 resolution

coding-tools 0.3.0 implements the desired model: one workspace runtime exposes
opaque `command_id` handles independently of transient HTTP/MCP request state.
The live ChatGPT-facing ElectroCAD plugin proved:

```text
exec_command -> command_id                     PASS
later write_stdin(command_id)                   PASS
later read_output(output_ref)                   PASS
later kill_command(command_id)                  PASS
retained output after termination               PASS
```

The 0.3.0 tool surface removes the need for persistent default-directory state;
clients should use explicit relative `workdir`, `cwd`, or path arguments.

## Manager public surface

Primary command:

```sh
workspace-mcp-manager instance client-compatibility <instance-id>
```

Legacy alias retained:

```sh
workspace-mcp-manager instance session-continuity <instance-id>
```

Projection versions:

```text
client_compatibility_projection_version = 2
session_continuity_projection_version = 3
```

## Qualification markers

```sh
bash scripts/qualify_p7_1_wsl.sh
```

Expected markers include:

```text
P7_1_ATOMIC_CODING_READINESS=PASS
P7_1_PROTOCOL_SESSION_PERSISTENCE=NOT_REQUIRED
P7_1_LOCAL_MCP_TRANSPORT_SESSION_MODEL=stateless
P7_1_LOCAL_MCP_COMMAND_HANDLES=cross_session
P7_1_LOCAL_TUNNEL_PROXY_COMPATIBILITY=PASS
P7_1_LOCAL_TUNNEL_PROXY_TRANSPORT_SESSION_MODEL=stateless
P7_1_LOCAL_TUNNEL_PROXY_COMMAND_HANDLES=cross_session
P7_1_DEFAULT_CWD_CROSS_CALL=OPTIONAL
P7_1_CROSS_CALL_RESOURCE_HANDLE_SCOPE=EXPLICIT_CAPABILITY
P7_1_IMPLEMENTATION_QUALIFICATION=PASS
```

The local tunnel-client dev proxy preserved the same stateless/cross-request
classification, confirming that forwarding does not introduce artificial
session affinity.

Additional qualification evidence for coding-tools 0.3.0 before canary apply:

```text
upstream make compliance: 127 tests, PASS
upstream make ci: PASS
focused MCP contract: 67 PASS
focused runtime semantics: 4 PASS
focused E2E: 6 PASS
focused tool golden: 9 PASS
focused security: 15 PASS
manager P7.1 direct/proxy qualification: PASS
manager PM3.1.2 live ElectroCAD qualification: PASS
manager source suite: 333 tests, OK (skipped=30)
PM3.1.1 -> PM3.1 -> PM3 -> PM2 regression unit: Result=success, ExecMainStatus=0
```
